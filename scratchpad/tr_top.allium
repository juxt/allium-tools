-- allium: 3

entity Job {
    status: pending | done
    transitions status { pending -> done  terminal: done }
}

rule R {
    when: Go(flag)
    ensures: Ghost.created(status: pending)
}

surface S {
    provides:
        Go(flag)
}
