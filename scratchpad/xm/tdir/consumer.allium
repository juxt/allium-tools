-- allium: 3

use "./t_dom.allium" as dom

rule Create {
    when: NewMember()
    ensures: dom/Membership.created(status: active)
}

rule AutoExpire {
    when: m: dom/Membership.expires_at <= now
    requires: m.status = active
    ensures: m.status = expired
}

surface Desk {
    provides:
        NewMember()
}
