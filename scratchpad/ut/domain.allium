-- allium: 3

entity Job {
    status: pending | done
    transitions status { pending -> done  terminal: done }
}

rule CreateJob {
    when: JobReq()
    ensures: Job.created(status: pending)
}

surface JobIntake {
    provides:
        JobReq()
}
